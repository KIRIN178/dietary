import { OnInit, ElementRef } from '@angular/core';
import { NavController, LoadingController } from '@ionic/angular';
import { ShareService } from "../services/data/share.service";
import { SuggestionService } from "../services/data/suggestion.service";
export declare class ThirdPage implements OnInit {
    loadingController: LoadingController;
    private ss;
    private data;
    navCtrl: NavController;
    private el;
    private name_goods;
    private name_categ;
    name_base: any[];
    private name_sub;
    private rowspan;
    private quantity;
    private amount;
    private unit_ingredient;
    private unit_ul;
    private rda_ai;
    private ul;
    private transfer_rda_ai;
    private transfer_ul;
    private goods_detail;
    private temp;
    private is_unit_correct;
    private init;
    private is_init;
    constructor(loadingController: LoadingController, ss: SuggestionService, data: ShareService, navCtrl: NavController, el: ElementRef);
    ngOnInit(): void;
    ngAfterViewChecked(): void;
    checkUnit(i: any, c: any, sub: any): boolean;
    checkUnitFinal(i: any): boolean;
    calColspan(i: any): 1 | 2;
    calc_taking_rda_ai(i: any): number | "";
    calc_taking_ul(i: any): number | "";
    detectOverdose(): boolean;
    detectSufficientdose(): boolean;
    getTemp(c: any): boolean;
    numberCheck(event: any, i: any): void;
    finalizeInitTemplate(): void;
    presentLoading(): Promise<void>;
}
